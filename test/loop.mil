BEGIN
        i := READ;
        IF i > 0 THEN step := 1 ELSE step := -1 FI;
        k := 0;
        WHILE k != i DO
                WRITE(k);
                k := k + step
        OD;
        WRITE(i)
END
        
